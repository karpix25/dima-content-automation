services:
  bot:
    build:
      context: .
      args:
        INSTALL_AUTH_TOOLS: ${INSTALL_AUTH_TOOLS:-false}
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./.data:/app/.data
      - ./outputs:/app/outputs
