services:
  bot:
    build:
      context: .
      args:
        INSTALL_AUTH_TOOLS: ${INSTALL_AUTH_TOOLS:-false}
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8000}:${WEB_PORT:-8000}"
    volumes:
      - ./.data:/app/.data
      - ./outputs:/app/outputs
